<%= form_with(model: section_material, local: true) do |form| %>
  <% if section_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section_material.errors.count, "error") %> prohibited this section_material from being saved:</h2>

      <ul>
      <% section_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :material_id %>
    <%= form.number_field :material_id %>
  </div>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.number_field :section_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
